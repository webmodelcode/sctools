import React from "react";
import ReactDOM from "react-dom/client";
import { SubtitleDisplay } from "~@/presentation/components/SubtitleDisplay/SubtitleDisplay";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SubtitleDisplay />
  </React.StrictMode>,
);
